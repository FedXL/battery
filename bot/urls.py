from django.urls import path
from rest_framework.routers import DefaultRouter
# from bot.views import WarehouseApiViewSet, CreateClientApiView, DataChoiceApiView, DeliveryTypeApiView, \
#     TaskApiViewSet, TaskDayApiViewSet

# router = DefaultRouter()
# router.register(r'warehouses', WarehouseApiViewSet, basename='warehouses')
# router.register(r'tasks', TaskApiViewSet, basename='tasks')
# router.register(r'task_dates', TaskDayApiViewSet, basename='task_dates')


# urlpatterns = [
#     path('new_user/', CreateClientApiView.as_view(), name='new_user'),
#     path("date_choice/", DataChoiceApiView.as_view(), name='date_choice'),
#     path("delivery_type/", DeliveryTypeApiView.as_view(), name='delivery_type'),
# ]

# urlpatterns += router.urls
